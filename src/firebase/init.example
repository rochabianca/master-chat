import firebase from 'firebase';
import firestore from 'firebase/firestore';

var config = {
  // copy here your firebase config
};
const firebaseApp = firebase.initializeApp(config);

export default firebase.firestore();
